import { NextFunction, Request, Response } from "express";

export const getLeaderBoard = (
  req: Request,
  res: Response,
  next: NextFunction,
) => {
  try {
  } catch (error) {
    console.log(error);
  }
};
